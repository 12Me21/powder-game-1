#include <stdio.h>
#include <complex.h>

double angle(_Complex double n) {
	double angle = acos(v->x/Vec_dist(v));
	if (v->y>0)
		angle = TAU-angle;

}


void main(void) {
	data[2][2] = 5;
	int a = (*((int(*)[10][20])test(1,1)))[1][1];
	printf("%d\n",a);
}
